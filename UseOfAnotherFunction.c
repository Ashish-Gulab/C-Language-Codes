#include<stdio.h>
int main()
{
  int a,b,c;
  printf("Enter the number of a and b");
  scanf("%d%d",&a,&b);
  c=((a!=b)&&(a<=b));
  printf("print value of %d",c);
}

/*
#include<stdio.h>
int argentina()
{
  int d,e,f;
  printf("Enter the number of a and b");
  scanf("%d%d",&d,&e);
  f=((d!=e)||(d<=e));
  printf("print value of %d",f);
}
*/
